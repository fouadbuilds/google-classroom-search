{
  "name": "google-classroom-search",
  "version": "2.0.0",
  "description": "A Chrome extension for Google Classroom with search functionality",
  "type": "module",
  "scripts": {
    "dev": "vite build && node scripts/copy-assets.js",
    "watch": "vite build --watch",
    "build": "vite build && node scripts/copy-assets.js",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "fuse.js": "^7.1.0"
  }
}
